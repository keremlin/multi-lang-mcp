{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "record_audio — input",
  "type": "object",
  "required": ["output_path"],
  "additionalProperties": false,
  "properties": {
    "output_path": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute or relative path where the WAV file will be saved"
    },
    "max_seconds": {
      "type": "number",
      "minimum": 1,
      "maximum": 120,
      "default": 15,
      "description": "Hard safety limit in seconds — recording always stops by this time (default 15)"
    },
    "silence_threshold": {
      "type": "number",
      "minimum": 0,
      "default": 1.5,
      "description": "RMS amplitude below which audio is considered silence on a 0–32767 scale (default 1.5). Raise in noisy environments."
    }
  }
}
