{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "record_audio — input",
  "type": "object",
  "required": ["output_path"],
  "additionalProperties": false,
  "properties": {
    "output_path": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute or relative path where the WAV file will be saved"
    },
    "max_seconds": {
      "type": "number",
      "minimum": 1,
      "maximum": 120,
      "default": 15,
      "description": "Hard safety limit in seconds — recording always stops by this time (default 15)"
    },
    "silence_duration": {
      "type": "number",
      "minimum": 0.3,
      "maximum": 10,
      "default": 1.5,
      "description": "Consecutive seconds of quiet after speech that triggers stop (default 1.5). Speech and silence thresholds are auto-calibrated from ambient noise."
    }
  }
}
